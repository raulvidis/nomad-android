---
description: "Safe commit with Conventional Commits."
argument-hint: "[scope/message hint]"
---
Stage explicit paths only (never `git add .`). Review !`git diff --staged`. Conventional Commit (feat|fix|refactor|build|ci|chore|docs|style|perf|test). No push unless asked. Hint: $ARGUMENTS
